UPSKILL — Student Certificates Page + Microcredential Title Fix v6

Apply this patch after the current v4 learning-flow patch. It includes the v5 student certificate-page optimization plus the certificate-title correction.

Student certificate page:
- Lists certificates vertically by microcredential name.
- Shows date received.
- View is a solid PSU navy button.
- Download is a white/navy-outline button.
- Buttons use modest 6px corners rather than pills.
- Certificate rows use modest 8px corners.
- Existing ownership and view/download routes remain unchanged.
- Mobile layout stacks actions cleanly.

Certificate content:
- The certificate's microcredential line now always uses courses.title.
- New certificates store the microcredential/course title instead of the generic "Certificate of Completion" as their title/snapshot.
- The document heading "CERTIFICATE of Completion" remains as the document type.
- Existing issued certificates created with the old generic title are repaired by a migration using their course title.
- PDF and browser certificate views use the microcredential title snapshot.
